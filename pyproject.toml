[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nanoforge"
version = "0.1.0"
description = "Educational LLM framework built from scratch."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths=["tests"]
